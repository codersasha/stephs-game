/**
 * Optional: worldwide unique usernames via Firebase Realtime Database.
 * 1) Create a Firebase project → Realtime Database → copy database URL
 *    (e.g. https://YOUR-PROJECT-default-rtdb.firebaseio.com)
 * 2) Set rules (see USERNAME_REGISTRY.md)
 * 3) Paste the URL below (no trailing slash).
 *
 * Leave empty to only reserve names on this device.
 */
window.WC_USERNAME_DB_URL = '';
